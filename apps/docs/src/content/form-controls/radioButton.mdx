---
title: "RadioButton"
summary: "RadioButton provides a selectable control for mutually exclusive options with customizable labels and styling."
updatedAt: "2026-07-27"
docs: "once-ui/form-controls/radioButton.mdx"
github: "components/RadioButton.tsx"
navLabel: "RadioButton"
navIcon: "switch"
navTag: "Update"
navTagVariant: "success"
---

The `RadioButton` component provides a selectable control for mutually exclusive options in a group. It's built on top of the `InteractiveDetails` component for consistent styling and behavior.

<CodeBlock
  marginTop="16"
  marginBottom="24"
  preview={
    <RadioButton
      name="subscription"
      value="monthly"
      label="Monthly subscription"
      description="Billed monthly at $9.99"
    />
  }
  codes={[
    {
      code:
`<RadioButton
    name="subscription"
    value="monthly"
    label="Monthly subscription"
    description="Billed monthly at $9.99"
    onToggle={() => console.log('Selected monthly')}
/>`,
      language: "tsx",
      label: "RadioButton"
    }
  ]}
/>

## State

Use the `checked` prop to control the radio button state externally.

<CodeBlock
  marginTop="16"
  marginBottom="24"
  preview={<SubscriptionRadioGroup />}
  codes={[
    {
      code:
`const [selectedPlan, setSelectedPlan] = useState<string>("monthly");

const handleToggle = (value: string) => {
    setSelectedPlan(value);
};

<Column gap="16">
    <RadioButton
      name="subscription"
      value="monthly"
      label="Monthly subscription"
      description="Billed monthly at $9.99"
      checked={selectedPlan === "monthly"}
      onToggle={() => handleToggle("monthly")}
    />
    <RadioButton
      name="subscription"
      value="annual"
      label="Annual subscription"
      description="Billed annually at $99.99"
      checked={selectedPlan === "annual"}
      onToggle={() => handleToggle("annual")}
    />
    <RadioButton
      name="subscription"
      value="lifetime"
      label="Lifetime access"
      description="One-time payment of $299.99"
      checked={selectedPlan === "lifetime"}
      onToggle={() => handleToggle("lifetime")}
    />
</Column>`,
      language: "tsx",
      label: "State"
    }
  ]}
/>

## Disabled

Disable the radio button to prevent user interaction.

<CodeBlock
  marginTop="16"
  marginBottom="24"
  preview={
    <RadioButton
      label="Disabled radio button"
      disabled
    />
  }
  codes={[
    {
      code:
`<RadioButton
    label="Disabled radio button"
    disabled
/>`,
      language: "tsx",
      label: "Disabled"
    }
  ]}
/>

## Hover

Disable the hover pulse animation by setting `hoverable` to `false`.

<CodeBlock
  marginTop="16"
  marginBottom="24"
  preview={
    <RadioButton
      label="No hover effect"
      hoverable={false}
    />
  }
  codes={[
    {
      code:
`<RadioButton
    label="No hover effect"
    hoverable={false}
/>`,
      language: "tsx",
      label: "Hover"
    }
  ]}
/>

## API reference

<PropsTable
  content={[
    ["checked", "boolean", "false"],
    ["onToggle", "() => void"],
    ["name", "string"],
    ["value", "string"],
    ["label", "ReactNode"],
    ["description", "ReactNode"],
    ["disabled", "boolean", "false"],
    ["hoverable", "boolean", "true"],
    ["className"],
    ["style"],
    ["iconButtonProps", "IconButtonProps", "-", "Props forwarded to the icon button beside the label."],
    ["...InputHTMLAttributes"]
  ]}
/>