---
title: "Input"
summary: "Input provides a flexible text entry field with support for labels, validation, and custom styling."
updatedAt: "2026-09-13"
docs: "once-ui/form-controls/input.mdx"
github: "components/Input.tsx"
navLabel: "Input"
navIcon: "switch"
---

import { ValidationInputExample, CharacterCountExample } from "../../product/InputExamples"

The `Input` component provides a text entry field with floating labels, validation support, and customizable styling. Use it for collecting user input in forms.

<CodeBlock
  marginTop="16"
  marginBottom="24"
  preview={
    <Input
      id="basic-input"
      label="Email"
      prefix={<Icon marginLeft="4" onBackground="neutral-weak" name="email" size="xs" />}
    />
  }
  codes={[
    {
      code:
`<Input
    id="input-1"
    label="Email"
    prefix={
      <Icon marginLeft="4" onBackground="neutral-weak" name="email" size="xs" />
    }
/>`,
      language: "tsx",
      label: "Input"
    }
  ]}
/>

## Error

The Input component supports error state and message separately. Access them with the `error` and `errorMessage` props.

<CodeBlock
  marginTop="16"
  marginBottom="24"
  preview={
    <Column fillWidth gap="16">
      <Input
        id="error-input"
        label="Email"
        error
      />
      <Input
        id="error-input"
        label="Password"
        error
        errorMessage={
          <Row vertical="center" gap="8">
            <Icon name="danger" size="xs" />
            Please enter at least 8 characters
          </Row>
        }
      />
    </Column>
  }
  codes={[
    {
      code:
`<Input
    id="input-1"
    label="Email"
    error
    errorMessage={
      <Row vertical="center" gap="8">
        <Icon name="danger" size="xs" />
        Please enter a valid email address
      </Row>
    }
/>`,
      language: "tsx",
      label: "Error"
    }
  ]}
/>

## Validation

The Input component supports custom validation through the `validate` prop. This prop accepts a function that receives the input value and returns either an error message or null. The validation is happens automatically on value change with an automatic debounce of 1 second.

<CodeBlock
  marginTop="16"
  marginBottom="24"
  preview={<ValidationInputExample />}
  codes={[
    {
      code:
`const [value, setValue] = useState("");

const validateEmail = (email: any) => {
    if (!email) return null;
    
    const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
    if (!emailRegex.test(email)) {
      return "Please enter a valid email address";
    }
    
    return null;
};

<Input
    id="input-1"
    value={value}
    onChange={(e) => setValue(e.target.value)}
    validate={validateEmail}
    placeholder="Email"
/>`,
      language: "tsx",
      label: "Validation"
    }
  ]}
/>

## Prefix & Suffix

Add icons or other elements before or after the input text.

<CodeBlock
  marginTop="16"
  marginBottom="24"
  preview={<SearchInput />}
  codes={[
    {
      code:
`const [searchValue, setSearchValue] = useState<string>("");

const handleChange = (e: React.ChangeEvent<HTMLInputElement>) => {
    setSearchValue(e.target.value);
};

const handleClear = () => {
    setSearchValue("");
};

<Input
    id="input-1"
    label="Search"
    value={searchValue}
    onChange={handleChange}
    prefix={<Icon name="search" size="xs" />}
    suffix={
      searchValue.length > 0 ? (
        <IconButton 
          variant="ghost" 
          icon="close" 
          size="s" 
          onClick={handleClear}
          aria-label="Clear search"
        />
      ) : null
    }
/>`,
      language: "tsx",
      label: "Prefix & Suffix"
    }
  ]}
/>

## Placeholder

Use the `placeholder` prop to use placeholder instead of label. The placeholder disappears when the input has value.

<CodeBlock
  marginTop="16"
  marginBottom="24"
  preview={
    <Input
      id="placeholder-input"
      placeholder="Name"
    />
  }
  codes={[
    {
      code:
`<Input
    id="input-1"
    placeholder="Name"
/>`,
      language: "tsx",
      label: "Placeholder"
    }
  ]}
/>

## Description

Use the `description` prop to add a description below the input.

<CodeBlock
  marginTop="16"
  marginBottom="24"
  preview={
    <Input
      id="description-input"
      label="Email"
      description={<Row vertical="center" gap="4">Provide your email address in the following format: <InlineCode>name@domain.com</InlineCode></Row>}
    />
  }
  codes={[
    {
      code:
`<Input
    id="input-1"
    label="Email"
    description="Provide your email address in the format name@domain.com"
/>`,
      language: "tsx",
      label: "Description"
    }
  ]}
/>

## Character count

Display a character counter with color-coded feedback based on remaining characters. The counter changes color when approaching the limit.

<CodeBlock
  marginTop="16"
  marginBottom="24"
  preview={<CharacterCountExample />}
  codes={[
    {
      code:
`const [bio, setBio] = useState<string>("");

const handleChange = (e: React.ChangeEvent<HTMLInputElement>) => {
    setBio(e.target.value);
};

<Input
    id="bio-input"
    placeholder="Bio"
    value={bio}
    onChange={handleChange}
    maxLength={100}
    characterCount
/>`,
      language: "tsx",
      label: "Character count"
    }
  ]}
/>

## Size

The Input component supports five sizes: extra small, small, medium, large, and extra large.

<CodeBlock
  marginTop="16"
  marginBottom="24"
  preview={
    <Column fillWidth gap="16">
      <Input id="size-xs" label="Extra Small" size="xs" />
      <Input id="size-s" label="Small" size="s" />
      <Input id="size-m" label="Medium" size="m" />
      <Input id="size-l" label="Large" size="l" />
      <Input id="size-xl" label="Extra Large" size="xl" />
    </Column>
  }
  codes={[
    {
      code:
`<Input placeholder="Extra Small" size="xs" />
<Input placeholder="Small" size="s" />
<Input placeholder="Medium" size="m" />
<Input placeholder="Large" size="l" />
<Input placeholder="Extra Large" size="xl" />`,
      language: "tsx",
      label: "Sizes"
    }
  ]}
 />

## On touch devices

iOS Safari zooms the page when a focused control computes below 16px, and it
does not reliably zoom back out — the page is left scrolled somewhere else.
Once UI's mobile base is 15px, which put three of the five sizes under that
threshold, `size="m"` among them.

On touch pointers only, the value is therefore clamped:

| | clamp |
| --- | --- |
| font-size | `max(token, 16px)` |
| line-height | `max(token, 1.3 × font-size)` |
| min-height (`xs`, `s`) | `40px` and `48px` |

Line-height is clamped alongside the font because `xs` and `s` resolve
`--line-height-body-s` to 16.88px on mobile — 16px glyphs would otherwise sit
in a line box barely taller than themselves.

Nothing changes on a mouse pointer, and `l` and `xl` are untouched either way:
they already cleared 16px. The visible consequence is that `xs`, `s` and `m`
render the same text size on a phone and differ by height alone, which is how
iOS treats its own text fields.

The floating label and the value are positioned from where their ink falls —
cap height, baseline and descender — so the pair stays optically centred at
whatever size and height the clamps produce.

## Variants

The Input component supports two variants: `default` and `ghost`.

<CodeBlock
  marginTop="16"
  marginBottom="24"
  preview={
    <Column fillWidth gap="16">
      <Input id="variant-default" label="Default" variant="default" />
      <Input id="variant-ghost" label="Ghost" variant="ghost" />
    </Column>
  }
  codes={[
    {
      code:
`<Input placeholder="Default" variant="default" />
<Input placeholder="Ghost" variant="ghost" />`,
      language: "tsx",
      label: "Variants"
    }
  ]}
 />

## Loading

Set `loading` to show a spinner in the input suffix while async validation or data fetching is in progress.

<CodeBlock
  marginTop="16"
  marginBottom="24"
  preview={
    <Input id="loading-input" label="Username" loading placeholder="Checking availability..." />
  }
  codes={[
    {
      code:
`<Input
  id="username"
  label="Username"
  loading
  placeholder="Checking availability..."
/>`,
      language: "tsx",
      label: "Loading"
    }
  ]}
/>

## Focus ring

Fields draw no focus ring by default — the quiet, borderless look modern native
inputs have settled on. Set `focusRing` to outline the field on focus, using the
same ring `Card` and `SmartLink` use, so a keyboard user can see where they are.
Worth turning on for any form someone has to tab through.

Note it shows on a mouse click too: `:focus-visible` always matches a field that
takes keyboard input, however that field was focused.

<CodeBlock
  marginTop="16"
  marginBottom="24"
  preview={
    <Column fillWidth gap="16">
      <Input id="ring-off" label="Default" placeholder="Click or tab in" />
      <Input id="ring-on" label="focusRing" focusRing placeholder="Click or tab in" />
    </Column>
  }
  codes={[
    {
      code:
`<Input id="email" label="Email" focusRing />`,
      language: "tsx",
      label: "Focus ring"
    }
  ]}
/>

## Built on Input

Four components build on `Input`. They keep everything on this page and add one
thing each, so they live on pages of their own:

- [NumberInput](/form-controls/numberInput) — steppers, `min`/`max`/`step`, and a numeric `onChange`
- [OTPInput](/form-controls/otpInput) — one box per digit, with paste and an `onComplete`
- [PasswordInput](/form-controls/passwordInput) — a reveal toggle
- [TagInput](/form-controls/tagInput) — an array of removable chips instead of a string

## API reference

<PropsTable
  component="Input"
  describe={{
    id: "Ties the label to the field. Required, and must be unique on the page.",
    label: "Floats above the value once the field is focused or filled.",
    placeholder: "Shown while the field is empty. Not a substitute for a label.",
    size: "Field height and type scale.",
    error: "Put the field in its error state.",
    errorMessage: "Message shown under the field when error is set.",
    description: "Helper text under the field, for the rule the label cannot carry.",
    corners: "Round only some corners \u2014 for stacking fields into one block.",
    prefix: "Rendered before the value: an icon, a currency symbol, a select.",
    suffix: "Rendered after the value: a unit, a clear button, a Kbd hint.",
    variant: "\"ghost\" drops the border and background, for a field inside a surface that already has them.",
    characterCount: "Show how much has been typed, against maxLength.",
    cursor: "Set \"interactive\" for a read-only field that opens something on click.",
    validate: "Run on a debounce; return a message to show, or null to clear.",
    loading: "Show a spinner in the suffix while something resolves.",
    className: "",
    style: "",
  }}
  content={[
    ["...InputHTMLAttributes"]
  ]}
/>