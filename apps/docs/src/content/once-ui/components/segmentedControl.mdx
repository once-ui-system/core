---
title: "SegmentedControl"
summary: "The SegmentedControl component allows users to select a single option from a set of horizontally arranged buttons. It supports keyboard navigation and customizable options."
updatedAt: "2025-05-09"
docs: "once-ui/components/segmentedControl.mdx"
github: "components/SegmentedControl.tsx"
navLabel: "Segmented Control"
navIcon: "components"
---

Use this component for toggle-style selection between discrete options. It supports keyboard interaction, styling, and controlled or uncontrolled usage.

<CodeBlock
  marginTop="16"
  marginBottom="24"
  preview={
    <SegmentedControl
      fillWidth={false}
      buttons={[
        { value: "day", label: "Day" },
        { value: "week", label: "Week" },
        { value: "month", label: "Month" },
      ]}
    />
  }
  codes={[
    {
      code:
`<SegmentedControl
    fillWidth={false}
    buttons={[
      { value: "day", label: "Day" },
      { value: "week", label: "Week" },
      { value: "month", label: "Month" },
    ]}
    onChange={(value) => console.log(value)}
/>`,
      language: "tsx",
      label: "Basic usage"
    }
  ]}
/>

## External state

Use `selected` to manage the selected state from the parent.

<CodeBlock
  marginTop="16"
  marginBottom="24"
  preview={
    <Row fillWidth horizontal="center">
      <SegmentedControl
        fillWidth={false}
        value="month"
        buttons={[
          { value: "day", label: "Day" },
          { value: "week", label: "Week" },
          { value: "month", label: "Month" },
        ]}
      />
      </Row>
  }
  codes={[
    {
      code:
`<SegmentedControl
    fillWidth={false}
    value="month"
    buttons={[
      { value: "day", label: "Day" },
      { value: "week", label: "Week" },
      { value: "month", label: "Month" },
    ]}
    onChange={(value) => console.log(value)}
/>`,
      language: "tsx",
      label: "External state"
    }
  ]}
/>

## Internal state

Use `defaultValue` to define an initial active button in uncontrolled mode. Don't use it along with the `value` prop.

<CodeBlock
  marginTop="16"
  marginBottom="24"
  preview={
    <Row fillWidth horizontal="center">
      <SegmentedControl
        fillWidth={false}
        defaultValue="week"
        buttons={[
          { value: "day", label: "Day" },
          { value: "week", label: "Week" },
          { value: "month", label: "Month" },
        ]}
      />
      </Row>
  }
  codes={[
    {
      code:
`<SegmentedControl
    fillWidth={false}
    defaultValue="week"
    buttons={[
      { value: "day", label: "Day" },
      { value: "week", label: "Week" },
      { value: "month", label: "Month" },
    ]}
    onChange={(value) => console.log(value)}
/>`,
      language: "tsx",
      label: "Internal state"
    }
  ]}
/>

## Compact

Use `compact` to create a compact variant.

<CodeBlock
  marginTop="16"
  marginBottom="24"
  preview={
    <Row fillWidth horizontal="center">
      <SegmentedControl
        compact
        defaultValue="week"
        buttons={[
          { value: "day", label: "Day" },
          { value: "week", label: "Week" },
          { value: "month", label: "Month" },
        ]}
      />
      </Row>
  }
  codes={[
    {
      code:
`<SegmentedControl
    compact
    defaultValue="week"
    buttons={[
      { value: "day", label: "Day" },
      { value: "week", label: "Week" },
      { value: "month", label: "Month" },
    ]}
    onChange={(value) => console.log(value)}
/>`,
      language: "tsx",
      label: "Compact"
    }
  ]}
/>

## Controlled state

Control the selected value externally with the `value` prop.

<CodeBlock
  marginTop="16"
  marginBottom="24"
  preview={
    <Row fillWidth horizontal="center">
      <SegmentedControl
        fillWidth={false}
        value="month"
        buttons={[
          { value: "day", label: "Day" },
          { value: "week", label: "Week" },
          { value: "month", label: "Month" },
        ]}
      />
    </Row>
  }
  codes={[
    {
      code:
`<SegmentedControl
  fillWidth={false}
  value="month"
  buttons={[
    { value: "day", label: "Day" },
    { value: "week", label: "Week" },
    { value: "month", label: "Month" },
  ]}
  onChange={(value) => console.log(value)}
/>`,
      language: "tsx",
      label: "Controlled"
    }
  ]}
/>

## Fill width

Distribute the button widths evenly across the container.

<CodeBlock
  marginTop="16"
  marginBottom="24"
  preview={
    <SegmentedControl
      buttons={[
        { value: "a", label: "A" },
        { value: "b", label: "B" },
        { value: "c", label: "C" },
      ]}
    />
  }
  codes={[
    {
      code:
`<SegmentedControl
  buttons={[
    { value: "a", label: "A" },
    { value: "b", label: "B" },
    { value: "c", label: "C" },
  ]}
  onChange={(value) => console.log(value)}
/>`,
      language: "tsx",
      label: "Fill width"
    }
  ]}
/>

## API reference

<PropsTable
  content={[
    ["buttons", "ButtonOption[]"],
    ["onChange", "(value: string, event?) => void"],
    ["defaultValue", "string"],
    ["value", "string"],
    ["fillWidth", "boolean", "true"],
    ["className"],
    ["style"],
    ["...scroller", "ScrollerProps"]
  ]}
/>
