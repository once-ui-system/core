---
title: "ContextMenu"
summary: "The ContextMenu component provides contextual options / information on right click."
updatedAt: "2025-07-31"
docs: "once-ui/components/contextMenu.mdx"
github: "components/ContextMenu.tsx"
navLabel: "ContextMenu"
navIcon: "components"
---

A component for providing users with contextual options / information on right click.

<CodeBlock
  marginTop="16"
  marginBottom="24"
  preview={
    <Column gap="16" fillWidth>
      <ContextMenu
        dropdown={
          <Column gap="2" padding="4" minWidth={10}>
            <Row paddingBottom="8" paddingTop="12" paddingX="16" onBackground="neutral-weak">
              <Text variant="label-default-s">Context Menu</Text>
            </Row>
            <Option
              prefix={<Icon size="xs" name="edit" onBackground="neutral-weak" />}
              label="Edit"
              value="edit"
            />
            <Option
              prefix={<Icon size="xs" name="document" onBackground="neutral-weak" />}
              label="Duplicate"
              value="duplicate"
            />
            <Option
              prefix={<Icon size="xs" name="copy" onBackground="neutral-weak" />}
              label="Copy"
              value="copy"
            />
            <Option
              prefix={<Icon size="xs" name="eyeOff" onBackground="neutral-weak" />}
              label="Hide"
              value="hide"
            />
            <Line marginY="2" />
            <Option
              prefix={<Icon size="xs" name="delete" onBackground="danger-medium" />}
              danger
              label="Delete"
              value="delete"
            />
          </Column>
        }
        placement="bottom-start"
      >
        <Row fillWidth align="center" horizontal="center" padding="40" radius="l" background="surface" border="neutral-alpha-medium" textVariant="label-default-m">Right click me</Row>
      </ContextMenu>
    </Column>
  }
  codes={[
    {
      code:
`<ContextMenu
    placement="bottom-start"
    dropdown={
      <Column gap="2" padding="4" minWidth={10}>
        <Option
          prefix={<Icon size="xs" name="edit" onBackground="neutral-weak" />}
          label="Edit"
          value="edit"
        />
        <Option
          prefix={<Icon size="xs" name="document" onBackground="neutral-weak" />}
          label="Duplicate"
          value="duplicate"
        />
        <Option
          prefix={<Icon size="xs" name="copy" onBackground="neutral-weak" />}
          label="Copy"
          value="copy"
        />
        <Option
          prefix={<Icon size="xs" name="eyeOff" onBackground="neutral-weak" />}
          label="Hide"
          value="hide"
        />
        <Line marginY="2" />
        <Option
          prefix={<Icon size="xs" name="delete" onBackground="danger-medium" />}
          danger
          label="Delete"
          value="delete"
        />
      </Column>
    }
>
    <Row fillWidth align="center" horizontal="center" padding="40" radius="l" background="surface" border="neutral-alpha-medium" textVariant="label-default-m">Right click me</Row>
</ContextMenu>`,
      language: "tsx",
      label: "ContextMenu"
    }
  ]}
/>

## API reference

<PropsTable
  component="ContextMenu"
  describe={{
    children: "The region that opens the menu on right-click.",
    dropdown: "The menu itself.",
    placement: "Where the menu opens relative to the pointer.",
    onSelect: "Called with the value of the Option that was picked.",
    selectedOption: "Value of the Option to mark as current.",
    closeAfterClick: "Close on selection.",
    open: "Controlled open state.",
    onOpenChange: "Called whenever it opens or closes.",
    disabled: "Stop the menu opening at all.",
    fillWidth: "Match the trigger's width.",
    minWidth: "Lower bound on the menu's width, in rem.",
    maxWidth: "Upper bound on the menu's width, in rem.",
    minHeight: "Lower bound on the menu's height, in rem.",
  }}
  content={[
    ["children"],
    ["className"],
    ["style"],
    ["...flex"]
  ]}
/>