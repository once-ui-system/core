---
title: "ParticleFx"
summary: "The ParticleFx component fills its area with drifting particles that can react to the pointer."
updatedAt: "2026-09-23"
docs: "once-ui/effects/particleFx.mdx"
github: "components/ParticleFx.tsx"
navLabel: "ParticleFx"
navIcon: "sparkle"
---

The `ParticleFx` component fills its area with drifting particles that can push away from or pull toward the pointer. It was called `Particle` before 2.0; that name still works as a deprecated alias until 3.0.

<CodeBlock
  marginTop="16"
  marginBottom="24"
  previewPadding="0"
  preview={
    <ParticleFx
      fill
      interactive
      speed={3}
      intensity={100}
      height={24}
    />
  }
  codes={[
    {
      code:
`<ParticleFx
    fill
    interactive
    speed={3}
    intensity={100}
    height={24}
/>`,
      language: "tsx",
      label: "ParticleFx"
    }
  ]}
/>

## Density

The `density` prop controls the number of particles to display.

<CodeBlock
  marginTop="16"
  marginBottom="24"
  previewPadding="0"
  preview={
    <ParticleFx
      fill
      interactive
      height={24}
      density={250}
    />
  }
  codes={[
    {
      code:
`<ParticleFx
    fill
    height={24}
    density={250}
/>`,
      language: "tsx",
      label: "Density"
    }
  ]}
/>

## Color

The `color` prop controls the color of the particles.

<CodeBlock
  marginTop="16"
  marginBottom="24"
  previewPadding="0"
  preview={
    <ParticleFx
      fill
      interactive
      height={24}
      color="accent-solid-medium"
    />
  }
  codes={[
    {
      code:
`<ParticleFx
    fill
    height={24}
    color="brand-background-strong"
/>`,
      language: "tsx",
      label: "Color"
    }
  ]}
/>

## Size

The `size` prop controls the size of the particles.

<CodeBlock
  marginTop="16"
  marginBottom="24"
  previewPadding="0"
  preview={
    <ParticleFx
      fill
      interactive
      height={24}
      size="8"
    />
  }
  codes={[
    {
      code:
`<ParticleFx
    fill
    height={24}
    size="8"
/>`,
      language: "tsx",
      label: "Size"
    }
  ]}
/>

## Speed

The `speed` prop controls the speed of the particles.

<CodeBlock
  marginTop="16"
  marginBottom="24"
  previewPadding="0"
  preview={
    <ParticleFx
      fill
      height={24}
      speed={5}
    />
  }
  codes={[
    {
      code:
`<ParticleFx
    fill
    height={24}
    speed={5}
/>`,
      language: "tsx",
      label: "Speed"
    }
  ]}
/>

## Interactive

The `interactive` prop controls whether the particles are interactive. Use the `intensity` prop to control the radius of the interaction area.

<CodeBlock
  marginTop="16"
  marginBottom="24"
  previewPadding="0"
  preview={
    <ParticleFx
      fill
      interactive
      height={24}
      intensity={100}
    />
  }
  codes={[
    {
      code:
`<ParticleFx
    fill
    height={24}
    interactive
/>`,
      language: "tsx",
      label: "Interactive"
    }
  ]}
/>

## Mode

The `mode` prop controls how particles interact with the cursor. Use `repel` to push particles away or `attract` to pull them towards the cursor.

<CodeBlock
  marginTop="16"
  marginBottom="24"
  previewPadding="0"
  preview={
    <ParticleFx
      fill
      interactive
      mode="attract"
      height={24}
      intensity={30}
      speed={2}
    />
  }
  codes={[
    {
      code:
`<ParticleFx
    fill
    interactive
    mode="attract"
    height={24}
    intensity={30}
/>`,
      language: "tsx",
      label: "Mode"
    }
  ]}
/>

## Opacity

The `opacity` prop controls the opacity of the particles.

<CodeBlock
  marginTop="16"
  marginBottom="24"
  previewPadding="0"
  preview={
    <ParticleFx
      fill
      interactive
      height={24}
      opacity={50}
    />
  }
  codes={[
    {
      code:
`<ParticleFx
    fill
    height={24}
    opacity={50}
/>`,
      language: "tsx",
      label: "Opacity"
    }
  ]}
/>

## Performance

ParticleFx automatically detects low-end devices and the `prefers-reduced-motion` system setting. When either is detected, particles are placed statically without animation. The animation also pauses when the element is outside the viewport. You can override this with the `reducedMotion` prop.

See [useReducedMotion](/hooks/useReducedMotion) for details.

## API reference

<PropsTable
  component="ParticleFx"
  describe={{
    density: "How many particles.",
    color: "Particle colour. Takes a token or any CSS colour.",
    size: "Particle size, on the spacing scale.",
    speed: "Drift multiplier. Not a duration.",
    interactive: "React to the pointer.",
    mode: "Whether the pointer pushes particles away or pulls them in.",
    intensity: "How strongly the pointer acts, when interactive.",
    opacity: "Layer opacity, in steps of 10.",
    reducedMotion: "\"auto\" follows the reader's system setting, which is the right default for anything ambient.",
    children: "Content drawn above the particles.",
  }}
  content={[
    ["className"],
    ["style"],
    ["children"]
  ]}
/>
