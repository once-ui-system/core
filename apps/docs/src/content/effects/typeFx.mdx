---
title: "TypeFx"
summary: "Animates text with a typewriter effect, typing and deleting characters one by one. Perfect for hero sections and dynamic headings."
updatedAt: "2025-10-14"
docs: "once-ui/effects/typeFx.mdx"
github: "components/TypeFx.tsx"
navLabel: "TypeFx"
navIcon: "sparkle"
---

TypeFx creates a realistic typewriter animation that types out text character-by-character. When multiple words are provided, it automatically types, holds, deletes, and cycles through them.

<CodeBlock
  marginTop="16"
  marginBottom="24"
  preview={
    <Row gap="8" vertical="center">
      <TypeFx
        words={["amazing products", "stunning websites", "modern apps"]}
        speed={80}
        hold={2000}
        trigger="instant"
      >Build{" "}</TypeFx>
    </Row>
  }
  codes={[
    {
      code:
`<Row gap="8" vertical="center">
  <Text variant="heading-default-l">
    Build
  </Text>
  <TypeFx
    words={["amazing products", "stunning websites", "modern apps"]}
    speed={80}
    hold={2000}
    trigger="instant"
  />
</Row>`,
      language: "tsx",
      label: "TypeFx"
    }
  ]}
/>

## Single word

TypeFx can also display a single word that types out once without looping.

<CodeBlock
  marginTop="16"
  marginBottom="24"
  reloadButton
  preview={
    <TypeFx
      words="Welcome to the future"
      speed={100}
      delay={500}
      trigger="instant"
    />
  }
  codes={[
    {
      code:
`<TypeFx
  words="Welcome to the future"
  speed={100}
  delay={500}
  trigger="instant"
/>`,
      language: "tsx",
      label: "Single word"
    }
  ]}
/>

## Custom trigger

Use the `custom` trigger to programmatically control when the typing animation starts.

<CodeBlock
  marginTop="16"
  marginBottom="24"
  preview={
    <Column gap="16">
      <TypeFxCustomExample />
    </Column>
  }
  codes={[
    {
      code:
`function TypeFxCustomExample() {
  const triggerFnRef = useRef<(() => void) | null>(null);

  return (
    <>
      <TypeFx
        words={["Click the button", "to start typing", "this animation"]}
        speed={60}
        hold={1500}
        trigger="custom"
        onTrigger={(fn) => {
          triggerFnRef.current = fn;
        }}
      />
      <Button onClick={() => triggerFnRef.current?.()}>
        Start typing
      </Button>
    </>
  );
}`,
      language: "tsx",
      label: "Custom trigger"
    }
  ]}
/>

## API reference

<PropsTable
  component="TypeFx"
  describe={{
    words: "One string to type, or several to cycle through.",
    speed: "Milliseconds per character.",
    delay: "Milliseconds before typing starts.",
    hold: "Milliseconds a finished word stays before the next one.",
    loop: "Cycle back to the first word instead of stopping.",
    trigger: "\"custom\" waits for the function handed to onTrigger.",
    onTrigger: "Receives a function that starts it, when trigger is \"custom\".",
    children: "Rendered after the typed text.",
  }}
  content={[
    ["...text", "TextProps"]
  ]}
/>
